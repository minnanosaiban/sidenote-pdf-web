name = "sidenote-pdf-web"
compatibility_date = "2026-06-01"
pages_build_output_dir = "public"

# このツールは完全にブラウザ内で完結する静的サイト。
# サーバーレス関数(functions/)も D1 も不要。
#
# 本人・関係者だけに限定公開したい場合は、court-calendar と同様に
# Cloudflare Access（Google ログイン）をダッシュボードで前段に噛ませる。
